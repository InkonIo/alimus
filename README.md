# Технический отчет: Pizza App

## 📋 Общая информация

**Название проекта:** Pizza App  
**Пакет:** com.example.alimus  
**Технологии:** Kotlin, Jetpack Compose, MVVM, LiveData  
**Платформа:** Android

---

## 🎯 Цель проекта

Разработать Android-приложение для отображения списка пицц с возможностью изменения количества каждой позиции. Приложение демонстрирует использование современных технологий Android-разработки и архитектурного паттерна MVVM.

---

## 🏗️ Архитектура проекта

### MVVM (Model-View-ViewModel)

Проект реализован по архитектурному паттерну **MVVM**, который разделяет приложение на три основных компонента:

#### 1. **Model (Модель)** - `Pizza.kt`
Отвечает за данные приложения. Содержит data class, описывающий структуру объекта "Пицца".

```
Свойства:
- id: Int - уникальный идентификатор
- name: String - название пиццы
- description: String - описание ингредиентов
- price: Double - цена
- imageUrl: String - ссылка на изображение (эмодзи)
- quantity: Int - текущее количество (по умолчанию 0)
```

#### 2. **View (Представление)** - `PizzaScreen.kt`
Отвечает за отображение UI. Написан на Jetpack Compose, реактивно реагирует на изменения данных из ViewModel.

#### 3. **ViewModel** - `PizzaViewModel.kt`
Связующее звено между Model и View. Содержит бизнес-логику и управляет состоянием UI через LiveData.

---

## 📁 Структура файлов

```
com.example.alimus/
│
├── MainActivity.kt                    # Точка входа приложения
│
├── data/
│   └── Pizza.kt                       # Модель данных
│
├── viewmodel/
│   └── PizzaViewModel.kt              # ViewModel с LiveData
│
└── ui/
    └── PizzaScreen.kt                 # UI на Jetpack Compose
```

---

## 🔧 Детальное описание компонентов

### 1. Pizza.kt (Model)

**Назначение:** Описание структуры данных для пиццы.

**Тип:** Data class

**Особенности:**
- Использует `data class` для автоматической генерации методов `equals()`, `hashCode()`, `toString()`, `copy()`
- Все свойства immutable (val), кроме `quantity` (var) для возможности изменения
- Значение по умолчанию для `quantity = 0`

**Преимущества data class:**
- Метод `copy()` позволяет создавать новые объекты с измененными свойствами (важно для immutable state)
- Автоматическая реализация методов сравнения

---

### 2. PizzaViewModel.kt (ViewModel)

**Назначение:** Управление данными и бизнес-логикой приложения.

**Наследование:** `ViewModel()` из AndroidX Lifecycle

**Компоненты:**

#### LiveData
```kotlin
private val _pizzas = MutableLiveData<List<Pizza>>()  // Приватная (для записи)
val pizzas: LiveData<List<Pizza>> = _pizzas            // Публичная (только чтение)
```

**Почему два поля?**
- `_pizzas` (MutableLiveData) - используется внутри ViewModel для изменения данных
- `pizzas` (LiveData) - expose наружу, UI может только читать, но не изменять напрямую
- Это паттерн **инкапсуляции** - защита от несанкционированных изменений

#### Методы:

**init {}**
- Вызывается при создании ViewModel
- Инициализирует список из 3 тестовых пицц
- Происходит один раз за жизненный цикл ViewModel

**increaseQuantity(pizzaId: Int)**
```kotlin
fun increaseQuantity(pizzaId: Int) {
    _pizzas.value = _pizzas.value?.map { pizza ->
        if (pizza.id == pizzaId) {
            pizza.copy(quantity = pizza.quantity + 1)
        } else {
            pizza
        }
    }
}
```
- Находит пиццу по `id`
- Создает новую копию объекта с увеличенным `quantity` (immutable approach)
- Обновляет LiveData → UI автоматически обновляется

**decreaseQuantity(pizzaId: Int)**
- Аналогично `increaseQuantity`, но уменьшает количество
- Проверка `quantity > 0` предотвращает отрицательные значения

**getTotalPrice(): Double**
- Вычисляет общую сумму заказа
- Формула: `sum(цена * количество)` для каждой пиццы
- Используется для отображения в корзине

**getTotalQuantity(): Int**
- Подсчитывает общее количество пицц в заказе
- Сумма всех `quantity` из списка
- Показывает сколько позиций в корзине

**clearCart()**
- Очищает корзину (сбрасывает все `quantity` в 0)
- Вызывается после "оплаты"
- Имитирует завершение заказа

**Важно:** Используется `copy()` вместо прямого изменения, потому что LiveData сравнивает ссылки на объекты. Создание новой копии гарантирует, что UI получит уведомление об изменении.

---

### 3. PizzaScreen.kt (View)

**Назначение:** Отображение UI и обработка пользовательских действий.

**Технология:** Jetpack Compose

#### Основные Composable-функции:

**PizzaScreen()**
```kotlin
@Composable
fun PizzaScreen(viewModel: PizzaViewModel = viewModel())
```
- Главная функция экрана
- Получает ViewModel через `viewModel()` - автоматическое создание/восстановление
- `observeAsState()` - конвертирует LiveData в Compose State
- Использует `Box` для наложения корзины поверх списка

**Важные состояния:**
```kotlin
val pizzas by viewModel.pizzas.observeAsState(emptyList())
var showPaymentDialog by remember { mutableStateOf(false) }
```
- `pizzas` - список пицц из ViewModel (LiveData)
- `showPaymentDialog` - локальное состояние для показа диалога оплаты

**Ключевой момент:**
```kotlin
val pizzas by viewModel.pizzas.observeAsState(emptyList())
```
- `observeAsState()` подписывается на изменения LiveData
- При изменении данных в ViewModel → автоматически перерисовывается UI
- `by` - делегирование свойства (kotlin delegation)
- `emptyList()` - значение по умолчанию, если LiveData null

**LazyColumn**
- Аналог RecyclerView, но для Compose
- Ленивая загрузка - создает только видимые элементы
- Эффективен для больших списков
- Добавлен `Spacer` в конце для отступа под корзину

**PizzaItem()**
- Отображает одну карточку пиццы
- Принимает коллбэки `onIncrease` и `onDecrease`
- При нажатии на кнопки вызываются методы ViewModel

**Структура карточки:**
```
Card
 └─ Row
     ├─ Text (эмодзи)
     ├─ Column (название, описание, цена)
     └─ Row (кнопки -, количество, +)
```

**Логика кнопки минус:**
```kotlin
enabled = pizza.quantity > 0
```
- Кнопка активна только если `quantity > 0`
- Визуально показывает, что дальше уменьшать нельзя

**CartBottomBar() - Корзина**
```kotlin
@Composable
fun CartBottomBar(
    totalPrice: Double,
    totalQuantity: Int,
    onCheckout: () -> Unit
)
```
- Отображается только если `totalQuantity > 0`
- Показывает общую сумму и количество товаров
- Кнопка "Оплатить" вызывает диалог
- Использует `Surface` с `shadowElevation` для эффекта поднятия

**Ключевая логика:**
```kotlin
if (totalQuantity > 0) { /* показываем корзину */ }
```
- Корзина появляется автоматически при добавлении товаров
- Исчезает когда корзина пустая

**PaymentDialog() - Диалог оплаты**
```kotlin
@Composable
fun PaymentDialog(
    totalPrice: Double,
    onDismiss: () -> Unit,
    onConfirm: () -> Unit
)
```
- `AlertDialog` - стандартный Material Design компонент
- Показывает итоговую сумму
- Два действия: "Подтвердить" и "Отмена"
- При подтверждении вызывает `clearCart()` из ViewModel
- Демо-версия: реальной оплаты не происходит

---

### 4. MainActivity.kt

**Назначение:** Точка входа приложения.

**Методы:**

**onCreate()**
```kotlin
setContent {
    MaterialTheme {
        PizzaScreen()
    }
}
```
- `setContent {}` - устанавливает Compose UI вместо XML layout
- `MaterialTheme` - применяет Material Design 3 стилизацию
- `PizzaScreen()` - запускает главный экран

---

## 🔄 Поток данных (Data Flow)

### Основной цикл работы с данными:

```
┌─────────────────────────────────────────────────────┐
│                   PizzaScreen (UI)                  │
│  - Отображает список пицц                           │
│  - observeAsState() слушает изменения               │
│  - Показывает корзину если есть товары              │
└─────────────────────────────────────────────────────┘
                         ▲
                         │ LiveData изменилась
                         │
┌─────────────────────────────────────────────────────┐
│              PizzaViewModel                         │
│  - Хранит LiveData<List<Pizza>>                     │
│  - Методы: increaseQuantity(), decreaseQuantity()  │
│  - Вычисляет: getTotalPrice(), getTotalQuantity()  │
└─────────────────────────────────────────────────────┘
                         ▲
                         │ Пользователь нажал кнопку
                         │
┌─────────────────────────────────────────────────────┐
│                   PizzaScreen (UI)                  │
│  - Кнопка + вызывает viewModel.increaseQuantity()  │
└─────────────────────────────────────────────────────┘
```

### Пример 1: Изменение количества

1. Пользователь нажимает кнопку "+" на карточке "Маргарита"
2. Вызывается `onClick = { viewModel.increaseQuantity(pizza.id) }`
3. ViewModel находит пиццу с нужным id
4. Создает новую копию списка с обновленным количеством
5. Обновляет `_pizzas.value`
6. LiveData уведомляет всех observers
7. `observeAsState()` получает уведомление
8. Compose перерисовывает только измененные элементы UI
9. На экране отображается новое количество
10. **Корзина автоматически пересчитывает сумму** (вызывает `getTotalPrice()`)

### Пример 2: Оформление заказа

```
Пользователь добавил пиццы → Корзина появилась внизу
                             ↓
              Нажал "Оплатить" → Появился PaymentDialog
                             ↓
           Подтвердил заказ → viewModel.clearCart()
                             ↓
         LiveData обновилась → UI перерисовался
                             ↓
       Все quantity = 0 → Корзина исчезла
```

### Реактивность корзины:

Корзина автоматически:
- **Появляется** когда `totalQuantity > 0`
- **Обновляет сумму** при изменении количества любой пиццы
- **Исчезает** когда корзина пустая (все quantity = 0)

Это происходит благодаря:
```kotlin
if (totalQuantity > 0) { /* показываем корзину */ }
```

При каждом изменении `pizzas` LiveData → пересчитываются `getTotalPrice()` и `getTotalQuantity()` → UI обновляется автоматически.

---

## 🛠️ Используемые технологии

### 1. **Kotlin**
- Современный язык для Android-разработки
- Data classes, Lambda expressions, Extension functions

### 2. **Jetpack Compose**
- Декларативный UI framework
- Реактивное обновление UI
- Меньше boilerplate кода по сравнению с XML

### 3. **ViewModel**
- Переживает пересоздание Activity (поворот экрана)
- Разделение логики и UI
- Часть Android Architecture Components

### 4. **LiveData**
- Observable паттерн
- Lifecycle-aware (не вызывает утечки памяти)
- Автоматическая отписка при уничтожении компонента

### 5. **Material Design 3**
- Современные UI компоненты (Card, Button, Text)
- Единый стиль приложения

---

## 📦 Зависимости (Dependencies)

```kotlin
// Compose BOM (Bill of Materials) - управление версиями Compose
implementation(platform("androidx.compose:compose-bom:2024.01.00"))

// Compose UI компоненты
implementation("androidx.compose.ui:ui")
implementation("androidx.compose.material3:material3")

// ViewModel для Compose
implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.7.0")

// LiveData интеграция с Compose
implementation("androidx.compose.runtime:runtime-livedata:1.6.0")
```

**Зачем BOM?**
- Автоматически синхронизирует версии всех Compose библиотек
- Предотвращает конфликты версий

---

## ✅ Преимущества архитектуры

### 1. **Разделение ответственности**
- Model: данные
- ViewModel: логика
- View: отображение

### 2. **Тестируемость**
- ViewModel можно тестировать без UI
- Бизнес-логика изолирована

### 3. **Переживает изменения конфигурации**
- При повороте экрана ViewModel сохраняется
- Данные не теряются

### 4. **Реактивность**
- UI автоматически обновляется при изменении данных
- Не нужно вручную обновлять View

### 5. **Масштабируемость**
- Легко добавить новые функции
- Можно заменить LiveData на StateFlow/Flow

---

## 🚀 Возможные улучшения

1. **Repository слой** - для работы с базой данных или API
2. **Dependency Injection** - Hilt/Koin для управления зависимостями
3. **Navigation Component** - для навигации между экранами
4. **StateFlow вместо LiveData** - более современный подход
5. **Room Database** - сохранение данных локально
6. **Coroutines** - для асинхронных операций

---

## 📝 Инструкция по запуску

### Шаг 1: Создать проект
- Android Studio → New Project → Empty Activity
- Package name: `com.example.alimus`

### Шаг 2: Настроить build.gradle.kts
- Добавить зависимости из файла `build.gradle.kts`
- Sync Project

### Шаг 3: Создать структуру пакетов
```
com.example.alimus/
├── data/
├── viewmodel/
└── ui/
```

### Шаг 4: Скопировать файлы
- `Pizza.kt` → в пакет `data`
- `PizzaViewModel.kt` → в пакет `viewmodel`
- `PizzaScreen.kt` → в пакет `ui`
- `MainActivity.kt` → в корень

### Шаг 5: Запустить
- Build → Make Project
- Run → Run 'app'

---

## 🎓 Ключевые концепции для понимания

### 1. **LiveData vs MutableLiveData**
- `MutableLiveData` = можно читать и писать
- `LiveData` = только читать
- Инкапсуляция защищает данные

### 2. **observeAsState()**
- Мост между LiveData (старый подход) и Compose State (новый)
- Автоматическая подписка/отписка
- Перерисовка UI при изменениях

### 3. **Immutability в Kotlin**
- `val` = нельзя переприсвоить
- `copy()` = создать новую копию с изменениями
- Важно для корректной работы LiveData

### 4. **Declarative UI (Compose)**
- Описываем "что должно быть", а не "как это сделать"
- UI = функция от state
- Автоматическая синхронизация

### 5. **Условный рендеринг**
```kotlin
if (totalQuantity > 0) { /* показываем корзину */ }
```
- UI элементы могут появляться/исчезать в зависимости от условий
- Корзина показывается только когда есть товары
- Это reactive programming - UI реагирует на изменения данных

### 6. **State hoisting (подъем состояния)**
- Диалог управляется состоянием `showPaymentDialog` в PizzaScreen
- Дочерние компоненты получают коллбэки (onCheckout, onConfirm)
- Single source of truth - одно место где хранится состояние

### 7. **remember и mutableStateOf**
```kotlin
var showPaymentDialog by remember { mutableStateOf(false) }
```
- `remember` - сохраняет значение между рекомпозициями
- `mutableStateOf` - создает observable state для Compose
- При изменении → автоматическая перерисовка

---

## 🔍 Часто задаваемые вопросы

**Q: Почему используется `copy()` вместо прямого изменения?**  
A: LiveData отслеживает изменения по ссылке на объект. Если изменить объект напрямую, ссылка останется той же, и UI не обновится. `copy()` создает новый объект → новая ссылка → UI обновляется.

**Q: Что такое `by` в `val pizzas by ...`?**  
A: Это делегирование свойства (property delegation). Позволяет получить значение напрямую, без вызова `.value`.

**Q: Зачем нужен ViewModel, если можно хранить данные в Composable?**  
A: ViewModel переживает пересоздание Activity (например, поворот экрана). Данные в Composable уничтожатся.

**Q: Можно ли использовать StateFlow вместо LiveData?**  
A: Да, StateFlow более современный подход. Но LiveData проще для начинающих и подходит для учебных проектов.

**Q: Почему корзина появляется/исчезает автоматически?**  
A: Используется условный рендеринг `if (totalQuantity > 0)`. При изменении LiveData → пересчитывается `totalQuantity` → условие проверяется заново → UI обновляется.

**Q: Где хранятся данные корзины?**  
A: Данные не хранятся отдельно. Корзина вычисляется на лету из списка пицц через `getTotalPrice()` и `getTotalQuantity()`. Это вычисляемые свойства (computed properties).

**Q: Что происходит после "оплаты"?**  
A: Вызывается `clearCart()` который обнуляет все `quantity`. LiveData обновляется → UI перерисовывается → корзина исчезает (т.к. `totalQuantity == 0`).

**Q: Как работает Box с Alignment.BottomCenter?**  
A: `Box` позволяет накладывать элементы друг на друга (z-axis). Корзина позиционируется внизу по центру поверх списка пицц.

---

## 📊 Итоги

Проект демонстрирует:
- ✅ Правильное применение MVVM архитектуры
- ✅ Использование LiveData для реактивности
- ✅ Современный UI на Jetpack Compose
- ✅ Разделение ответственности между компонентами
- ✅ Best practices Android-разработки
- ✅ Условный рендеринг UI элементов
- ✅ Работа с вычисляемыми свойствами (getTotalPrice, getTotalQuantity)
- ✅ Использование AlertDialog для подтверждения действий
- ✅ State management через remember и mutableStateOf

**Время разработки:** ~40-50 минут  
**Сложность:** Начальный/Средний уровень  
**Строк кода:** ~300

---

## 📚 Полезные ресурсы

- [Официальная документация Jetpack Compose](https://developer.android.com/jetpack/compose)
- [ViewModel Overview](https://developer.android.com/topic/libraries/architecture/viewmodel)
- [LiveData Overview](https://developer.android.com/topic/libraries/architecture/livedata)
- [MVVM Architecture](https://developer.android.com/topic/architecture)

---

**Дата создания отчета:** Декабрь 2024  
**Версия:** 1.0
